// import { fromJS } from 'immutable';
// import { selectTagDomain } from '../selectors';

describe('selectTagDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
