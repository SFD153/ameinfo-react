// import { fromJS } from 'immutable';
// import { selectAuthorDomain } from '../selectors';

describe('selectAuthorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
