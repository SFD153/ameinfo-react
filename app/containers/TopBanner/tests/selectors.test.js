// import { fromJS } from 'immutable';
// import { selectTopBannerDomain } from '../selectors';

describe('selectTopBannerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
