// import { fromJS } from 'immutable';
// import { selectContentLockerDomain } from '../selectors';

describe('selectContentLockerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
