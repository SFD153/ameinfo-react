// import { fromJS } from 'immutable';
// import { selectMobileMenuDomain } from '../selectors';

describe('selectMobileMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
