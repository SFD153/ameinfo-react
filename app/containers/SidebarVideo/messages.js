/*
 * SidebarVideo Messages
 *
 * This contains all the text for the SidebarVideo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SidebarVideo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SidebarVideo container!',
  },
});
