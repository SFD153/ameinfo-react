// import { fromJS } from 'immutable';
// import { selectFullScreenSearchDomain } from '../selectors';

describe('selectFullScreenSearchDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
